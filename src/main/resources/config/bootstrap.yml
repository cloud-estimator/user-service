spring:
  application:
    name: user-service 
    
  cloud:
    config:
      enabled: false

---

spring:
  profiles: cloud
  cloud:
    config:
      enabled: true
      uri: ${CONFIGSERVER_URI:http://localhost:8888}
      fail-fast: true
    
    
